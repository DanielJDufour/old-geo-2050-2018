{
  "name": "geo-2050-2018",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "react-scripts": "1.0.17",
    "underscore": "^1.8.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && rm -fr docs && cp build docs -fr && cd docs && touch .nojekyll && touch CNAME && echo '2018.geo2050.org' > CNAME && cd .. && zip -r geo-2050-2018-build.zip docs"
  }
}
